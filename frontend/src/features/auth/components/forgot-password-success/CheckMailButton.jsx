import Button from "@/shared/components/app-components/Button";

const CheckMailButton = ({ onClick }) => {
  return <Button title="CHECK IN MAIL" className="w-full" onClick={onClick} />;
};

export default CheckMailButton;
